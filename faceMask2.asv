function mask = faceMask2(imIn)
    
    imIn = AWB(imIn,6);
    imGray = rgb2gray(imIn);

    mask = imGray > 0.8;
    
    if mean(mean(mask)) > 0.5
        mask = 
    end    

    mask = dilationDisk(mask,6);
    mask = dilationDisk(mask,6);
    mask = erodationDisk(mask,6);
    mask = erodationDisk(mask,6);
end
